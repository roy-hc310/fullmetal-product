version: '3.8'

services:
  app:
    deploy:
      resoures:
        limits:
          cpus: '0.5'
          memory: 256m
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "50051:50051"
    env_file: .env